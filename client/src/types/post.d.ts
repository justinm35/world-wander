interface IPosts {
    destination: string,
    dateTraveled?: string,
    tripLength?: string,
    description: string,
    destCoordinates?: object
    creator: string,
    createdAt: string,
    _id?: string,
    photos: string[]
  }